[Unit]
Description=Share the current X session via VNC
After=display-manager.service

[Service]
Type=simple
ExecStart=/usr/bin/x11vnc \
  -display :0 \
  -auth guess \
  -rfbauth /root/.vnc/passwd \
  -forever \
  -noxdamage \
  -repeat \
  -shared \
  -rfbport 5900
Restart=on-failure

[Install]
WantedBy=graphical.target